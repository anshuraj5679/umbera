# deps
node_modules/
.pnpm-store/
**/package-lock.json

# env
.env
.env.local
.env.*.local
!.env.example

# build artifacts
dist/
build/
out/
.next/
artifacts/
cache/
typechain-types/
forge-out/
contracts/lib/forge-std/

# coverage
coverage/
lcov.info

# OS / editor
.DS_Store
.idea/
.vscode/
*.log

# deployed addresses for ephemeral envs (committed for arbSepolia/local)
shared/addresses/.deployed-ephemeral-*.json

# generated ABIs (output of contracts/scripts/export-abi.ts)
shared/abi/*.json
!shared/abi/.gitkeep
!shared/abi/*.json

# TypeScript source is canonical for the shared package.
shared/auction/*.js
shared/addresses/index.js

# Private state stores & build caches
*-private-store/
*.tsbuildinfo
*.hex
*.bin
tree_updates*.json

.vercel
