{
  "name": "@umbra/contracts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "compact compile midnight/src/umbra.compact --out-dir midnight/dist",
    "compile": "compact compile midnight/src/umbra.compact --out-dir midnight/dist"
  },
  "devDependencies": {
    "@midnight-ntwrk/compactc": "^0.8.0"
  }
}
