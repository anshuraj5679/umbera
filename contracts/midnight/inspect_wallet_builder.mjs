import { WalletBuilder } from '@midnight-ntwrk/wallet';
console.log('WalletBuilder methods:', Object.getOwnPropertyNames(WalletBuilder));
console.log('WalletBuilder.build:', WalletBuilder.build.toString().slice(0, 500));
